Row Generators
==============

Row generators are iterator objects that are created from the targets of AppUrls. They have special support
for row headers and can iterate through row data as  rows, dicts, or RowProxy objects.